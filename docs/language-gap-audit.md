# Language Gap Audit

Status: first pass plus one portable function port.

This audit will track differences between upstream MMB4L and the current
PicoMite MMBasic v6 language/runtime. Do not treat a difference as a bug until
it is classified.

## Baseline

| Item | Value |
| --- | --- |
| MMB4L upstream remote | `https://github.com/thwill1000/mmb4l` |
| PicoMite reference tree | `C:\Users\georg\Codex_Projects\WebMite\PicoMiteAllVersions` |
| Working branch | `language-v6-port-docs` |
| Hardware scope | Excluded for now |

## First Pass Counts

Generated by comparing normalized names from:

- MMB4L `src/core/commandtbl.c`
- MMB4L `src/core/tokentbl.c`
- PicoMite `AllCommands.h`

| Source | Count |
| --- | ---: |
| MMB4L command table entries | 122 |
| MMB4L token table entries | 121 |
| PicoMite `AllCommands.h` entries | 362 |
| PicoMite entries already represented in MMB4L | 222 |
| PicoMite entries missing from MMB4L by normalized name | 134 |

This is a rough inventory. Some rows are aliases, variant-gated features, or
commands that are only meaningful on PicoMite hardware. Each candidate still
needs source review before it becomes a porting task.

## Classification

| Gap | Classification | Reference | MMB4L location | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| `Trim$(` | portable | PicoMite `AllCommands.h`, `Functions.c` | `src/core/Functions.c`, `src/core/tokentbl.c`, `tests/tst_strings.bas` | patched | String-only function; no hardware dependency. Supports PicoMite-style default left trim plus optional mask and `L`/`R`/`B` side argument. |
| ARMv7 architecture label | portable | N/A | `src/main.c` | candidate | Existing Luckfox patch changes reported banner text from ARMv6 to ARMv7 when compiled for ARMv7. |
| PicoCalc framebuffer scaling/DirectFB behavior | hardware/linux-backend | N/A | Luckfox project | out of scope | Keep in `mmbasic-4-luckfox-lyra`; not part of language v6 work. |
| GPIO/SPI/I2C commands | hardware | PicoMite hardware modules | N/A | out of scope | No current use case. |

## Candidate Buckets

Portable-looking candidates that need source review:

| Candidate | Initial classification | Notes |
| --- | --- | --- |
| `SChange$(` | portable | String function; likely a good next audit target. |
| `base$(` | portable | String/numeric conversion helper; review behavior and name casing. |
| `Save` | linux-specific | MMB4L intentionally treats files differently from PicoMite flash; needs design discussion. |
| `Flush` | portable/linux-specific | May map to Linux file flushing if semantics are compatible. |
| `ReDim` | portable | Language/runtime feature; needs array semantics review. |
| `LMid(` | portable | LongString command/function; review against existing MMB4L LongString support. |
| `/*` and `*/` | portable | Multiline comment tokens exist in PicoMite; review parser impact. |

Hardware or backend candidates to keep out of this fork for now:

| Candidate group | Reason |
| --- | --- |
| GPIO, ADC, PWM, IR, RTC, Servo, Stepper | Hardware-facing PicoMite runtime. |
| I2C, SPI, OneWire, PIO | Hardware bus/PIO surface; no current use case. |
| Touch, Backlight, LCD, Camera, Tile/Tilemap, Draw3D, Ray | Display/input/backend-specific. |
| Web, GPS, FM, Bitstream | Variant or subsystem work, not core language alignment. |
| CMM2 load/run, update firmware | Embedded/firmware workflow, not Linux language runtime. |

## Current Verification

Before implementation, `tests/tst_strings.bas` failed with:

```text
Error in line 199: TRIM is not declared
```

After implementation:

```text
test_trim_function: PASS (7/7)
tests/tst_strings.bas: PASS (15/15)
tests/tst_strings.bas --base=1: PASS (15/15)
```

The project directory was recreated with Windows case sensitivity enabled, so
the full `/mnt/c` host build and CTest suite now pass:

```text
100% tests passed, 0 tests failed out of 999
The following tests did not run:
  581 - ParseTest.ParsePage_GivenUnknownNonStringPageId_AndPicomite (Skipped)
```

## Audit Procedure

1. Extract MMB4L command, function, and operator tables.
2. Extract PicoMite command, function, and operator definitions.
3. Compare names and syntax.
4. Read source for differences that are behavior-compatible but named
   differently.
5. Add one row per candidate gap before writing code.

## Notes

MMB4L already documents intentional differences from PicoMite in its README.
Those differences should be reviewed before classifying a missing behavior as a
porting task.
